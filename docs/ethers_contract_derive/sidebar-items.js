initSidebarItems({"derive":[["EthAbiCodec","Derives the `AbiEncode`, `AbiDecode` and traits for the labeled type."],["EthAbiType","Derives the `AbiType` and all `Tokenizable` traits for the labeled type."],["EthCall","Derives the `EthCall` and `Tokenizeable` trait for the labeled type."],["EthDisplay","Derives `fmt::Display` trait and generates a convenient format for all the underlying primitive types/tokens."],["EthEvent","Derives the `EthEvent` and `Tokenizeable` trait for the labeled type."]],"macro":[["abigen","Proc macro to generate type-safe bindings to a contract(s). This macro accepts one or more Ethereum contract ABI or a path. Note that relative paths are rooted in the crate’s root `CARGO_MANIFEST_DIR`. Environment variable interpolation is supported via `$` prefix, like `\"$CARGO_MANIFEST_DIR/contracts/c.json\"`"]]});